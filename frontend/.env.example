# Only needed if the frontend and backend are on different domains.
# When both are served through the same nginx (default docker-compose setup),
# leave this unset and the app will call same-origin /api automatically.
# VITE_API_URL=https://api.yourdomain.com/api
